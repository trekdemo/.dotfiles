#!/bin/sh
#
# Run specs with great speed and daring-do.
#
clear

if [ -S .zeus.sock ]; then
  zeus rspec "$@"
else
  if [ -f .bin/rspec ]; then
    rspec "$@" -d
  else
    bundle exec rspec "$@" -d
  fi
fi

