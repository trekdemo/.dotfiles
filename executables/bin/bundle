#!/bin/bash

if test -f ./docker-compose.yml; then
  CMD = "docker-compose run --rm web bundle $*"
else
  BUNDLE_BIN=$(which bundle)
  if [ "$0" = "$BUNDLE_BIN" ]; then
    echo 'Command bundle points to invalid executable!'
    exit 1
  fi
  CMD="$BUNDLE_BIN $*"
fi

echo $CMD
eval $CMD
